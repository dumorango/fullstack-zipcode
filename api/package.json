{
  "name": "api",
  "version": "1.0.0",
  "description": "Zipcode GraphQL API",
  "main": "index.js",
  "scripts": {
    "compile:watch": "tsc --skipLibCheck -w src/index.ts",
    "run:watch":"nodemon src/index.js",
    "start": "run-p compile:watch run:watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^3.6.7",
    "graphql": "^16.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  }
}
